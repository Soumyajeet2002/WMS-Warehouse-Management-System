import { Box, Typography } from '@mui/material';

const NotFound = () => {
    return (
        <Box>
            <Typography variant="h4">
                404
            </Typography>

            <Typography>
                Page not found.
            </Typography>
        </Box>
    );
};

export default NotFound;